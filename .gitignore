# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Disparo de e-mail: credenciais, base de contatos e previews nunca vão pro repo
scripts/email-fundadores/.env
scripts/email-fundadores/*.csv
scripts/email-fundadores/preview/
scripts/email-fundadores/enviados.json
emails/preview/

# KPV: credenciais das APIs de preco (Mercado Livre, eBay). Ficam so na maquina.
.env.kpv
.env.kpv.*

# Documentacao de trabalho fica so na maquina: o repositorio e publico e esses
# arquivos carregam dado de negocio (pendencias, estrategia) e ate e-mail de
# vendedor. As excecoes sao o README, que e a cara do repositorio, e o .agent/,
# que e configuracao de ferramenta e nao documentacao.
*.md
!README.md
!.agent/**/*.md
.vercel

# KPV: dados coletados das fontes externas ficam so na maquina
scripts/kpv/dados/

# MCP local (contém tokens/segredos — nunca versionar)
.mcp.json
